---
---
.excerpt
  - url = "#{route_path(item)}"
  %h2 
    %a(href="#{url}" title="Full article" class="permalink")= "&laquo; #{item[:title]}"
  .body
    %p
      = item[:excerpt] || blog_excerpt(item) || "!!! Missing excerpt metadata for <code>#{item[:file].path}</code>"
    %p
      = item[:created_at]
  -# = render('_meta', :item => item)
    
